import React from 'react';

/**
 * Display a message to the user. 
 * @param {*} props 
 */
const ContactSucces = (props) => {
    return (<div><p>Thank you {props.name} .. We will get back to you ASAP</p></div>);
}

export default ContactSucces; 